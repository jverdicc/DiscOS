# Changelog

## 0.1.0
- Initial shared verifier crate for EvidenceOS/DiscOS.
- Adds transcript helpers (`sha256_domain`, `etl_leaf_hash`, STH/revocation digests).
- Adds ETL inclusion and consistency proof verification primitives.
